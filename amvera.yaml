meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  commands:
    - node server/server.js

port: 3000

env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: "3000"